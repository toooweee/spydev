
import { ThemeProvider } from '@mui/material';
import AppTheme from '../theme';
export function App() {
  return (
    <ThemeProvider theme={AppTheme}>

    </ThemeProvider>
  );
}

export default App;
